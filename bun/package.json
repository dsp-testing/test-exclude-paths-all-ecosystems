{
  "name": "bun-workspace-root",
  "version": "1.0.0",
  "private": true,
  "description": "Bun workspace for testing Dependabot exclude-paths",
  "workspaces": [
    "include-me",
    "exclude-me"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "dev": "bun run --filter '*' dev",
    "install:all": "bun install"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.45.0",
    "typescript": "^5.0.0",
    "bun-types": "^1.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
